Vagrant.configure("2") do |config|
    config.vm.box = "ubuntu/jammy64"
    config.vm.provider "virtualbox" do |vb|
        vb.memory = "1024"  # 1 GB
        vb.cpus = 2
    end

    config.vm.network "private_network", ip: "192.168.1.140"
    config.vm.network "public_network"
end